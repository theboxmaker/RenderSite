RewriteEngine On

# If the request is a real file or folder, serve it normally
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite /alpha → /index.php?page=alpha
RewriteRule ^([^/]+)/?$ index.php?page=$1 [L,QSA]
